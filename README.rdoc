= has_money
In your active record model:
  class Order < ActiveRecord::Base
    has_money :default_price
  end
  
Allows you to use 
  
  def default_price_in_dollars
  end

  def default_price_in_dollars=(dollars)
  end